import { BirthSky } from "@/features/kundali/components/birth-sky";

export const metadata = {
  title: "The Sky at Birth",
  robots: { index: false },
};

export default function SkyPage() {
  return <BirthSky />;
}
