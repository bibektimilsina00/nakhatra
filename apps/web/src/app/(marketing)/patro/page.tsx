import { PatroPage } from "@/features/patro/components/patro-page";

export const metadata = {
  title: "नेपाली पात्रो · Nepali Patro — Nakhatra",
  description:
    "The Bikram Sambat month with each day's tithi, festivals, sunrise and moonrise, and the full panchang — computed sidereally, read at each day's own sunrise.",
};

export default function Page() {
  return <PatroPage />;
}
